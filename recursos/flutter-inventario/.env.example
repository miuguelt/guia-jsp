# =========================================================================
# Configuración de entorno — Flutter Inventario
# =========================================================================
# ⚠️  En Flutter las variables se inyectan en BUILD time via --dart-define
# ⚠️  Este archivo es solo referencia. No tiene efecto en runtime.
# En Coolify se usa la variable API_BASE_URL durante el build.
# =========================================================================

# URL base de la API de Inventario (FastAPI)
API_BASE_URL=http://localhost:8000/api
